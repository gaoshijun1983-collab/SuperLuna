[project]
name = "superluna"
version = "0.2.0a95"
description = "A safe Codex and ChatGPT development review loop"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-directory = "tests"
